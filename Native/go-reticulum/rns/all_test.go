package rns

import "testing"

// This file mirrors `tests/all.py` as a simple aggregation point.
// Go's `go test ./...` will discover and run all tests regardless,
// but having a single named test helps parity and CI targeting.
func TestAll(t *testing.T) {
	// Intentionally empty.
}

