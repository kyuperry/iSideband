# crypto parity TODO

Only items still outstanding vs `python/RNS/Cryptography/Provider.py` and related wrapper helpers in `python/RNS/Cryptography/__init__.py`.

## TODO (remaining parity gaps)

- Confirm wrapper API surface matches the Python module-level helpers expected by higher layers (naming and defaults).
- Verify HKDF wrapper semantics (`HKDFSHA256/512`) align with Python usage patterns (salt defaulting, info/context handling).

