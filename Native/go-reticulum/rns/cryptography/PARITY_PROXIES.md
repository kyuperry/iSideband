# proxies parity TODO

Only items still outstanding vs `python/RNS/Cryptography/Proxies.py`.

## TODO (remaining parity gaps)

- Verify the proxy API surface is sufficient for all consumers (Generate/FromBytes/PublicBytes/Exchange/Sign/Verify).
- Confirm key byte formats match Python’s proxy expectations (ed25519 private key bytes vs seed semantics).

